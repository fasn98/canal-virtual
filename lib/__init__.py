# Código compartilhado do projeto canal-virtual.
#
# Módulos aqui NÃO dependem de nenhum serviço específico e são pensados para
# ser importados por qualquer um deles (ou executados solto para teste manual).
# Quando um serviço passar a usar um módulo daqui, copie/monte `lib/` no
# contexto de build da imagem daquele serviço (hoje cada Dockerfile builda só
# a própria pasta).
